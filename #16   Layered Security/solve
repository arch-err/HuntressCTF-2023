file layered_security
Its a gimp file (probably a project file)
Open it in gimp, there are multiple layers in the file
Go through the layers, wow, the flag is right there

Im lazy, so screenshot and tesseract did the job (really need to revive that ocr project...)
